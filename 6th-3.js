var arr = new Set ([2,3,7,5,2,6,9,7,0])
    let max = arr[0];
    for (let i of arr.keys())
    {
        i = this.arr
        for (let i = 0;i < arr.length;i++)
        {
            if (arr[i] >= max)    
            let max = arr[i];
        }
            //i = this.arr;
            //i <= arr.length;
            //if (arr[i] > max)
                //{     
                //    max = arr[i];
                //}
            //i++;
    return max;
    }
arr.delete(max);

let map = new Map ();
map.set ('张三',19)
map.set ('李四',18)
map.set ('王麻子',20)
for (let sum of map.values())
    {
        sum
    }

